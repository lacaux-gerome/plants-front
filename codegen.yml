overwrite: true
schema: "http://localhost:4000/graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "fragment-matcher"
  ./graphql.schema.json:
    plugins:
      - "introspection"
